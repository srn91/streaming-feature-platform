# Observability Views

Track these signals from the `/metrics` endpoint:

- request rate and latency by path
- raw event ingestion volume
- feature snapshot materialization count
- training dataset row count
- quality summary freshness and reconciliation status

Use the Prometheus scrape config in the same directory as the source for a Grafana or managed dashboard.
